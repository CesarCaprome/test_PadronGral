function goMonth(date, offset) {
    const d = new Date(date);
    d.setMonth(d.getMonth() + offset);
    return d;
}

var hoy = new Date(),
	dDesde = HS.GOMONTH(hoy,-4),
	dDesde = new Date(dDesde.getFullYear(),dDesde.getMonth(),1);
 
config = config || {};
 
Ext.apply(config, {
	emptyText: Ext.util.Format.date(dDesde, 'd/m/Y'),
	value: dDesde,
	minValue: HS.GOMONTH(dDesde, -18),
	maxValue: hoy
});
 
return config;






 processTxtFecha: function(config) {
        var hoy = new Date(),
        	dDesde = HS.GOMONTH(hoy,-4),
 
 
    hoy = new Date(hoy.getFullYear(), hoy.getMonth(), hoy.getDate()); // Hoy a las 00:00

    // Fecha 18 meses atrás
    var dDesde = new Date(hoy);
    dDesde.setMonth(dDesde.getMonth() - 18);
    dDesde = new Date(dDesde.getFullYear(), dDesde.getMonth(), 1); // Primer día del mes

    config = config || {};

    Ext.apply(config, {
        emptyText: Ext.util.Format.date(dDesde, 'd/m/Y'), // Texto gris
        value: hoy,        // Valor por defecto
        minValue: dDesde,  // Desde 18 meses atrás
        maxValue: hoy      // Hasta hoy
    });

    return config;
	
	
	
	
Codigo Gonza Process to fech	
	/*

var hoy = new Date(),
	dDesde = HS.GOMONTH(hoy,-4),
	dDesde = new Date(dDesde.getFullYear(),dDesde.getMonth(),1);
 
config = config || {};
 
Ext.apply(config, {
	emptyText: Ext.util.Format.date(dDesde, 'd/m/Y'),
	value: dDesde,
	minValue: HS.GOMONTH(dDesde, -18),
	maxValue: hoy
});
 
return config
*/*/
